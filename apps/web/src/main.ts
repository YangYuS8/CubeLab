const app = document.querySelector<HTMLDivElement>('#app');

if (app) {
  app.innerHTML = `
    <main style="font-family: Inter, system-ui, sans-serif; margin: 3rem auto; max-width: 48rem; padding: 0 1rem; line-height: 1.6;">
      <h1>CubeLab</h1>
      <p>Web application placeholder for learner and instructor experiences.</p>
      <p>This app intentionally contains only a minimal shell during repository initialization.</p>
    </main>
  `;
}
